{
  "name": "svelte-rxd-preprocessor",
  "version": "0.1.3",
  "description": "Wrap svelte reactive statements with custom events to allow devtools to detect them",
  "main": "index.js",
  "scripts": {
    "test": "DIR=tests/app && $npm_execpath install --prefix $DIR && $npm_execpath run dev --prefix $DIR & cypress run"
  },
  "keywords": [
    "svelte",
    "reactive",
    "debugger"
  ],
  "author": "unlocomqx",
  "license": "ISC",
  "dependencies": {
    "acorn": "^8.0.4",
    "escape-string-regexp": "^4.0.0",
    "json-stringify-safe": "^5.0.1",
    "linenumber": "^1.0.1",
    "periscopic": "^2.0.3"
  },
  "devDependencies": {
    "cypress": "^6.2.1"
  }
}
